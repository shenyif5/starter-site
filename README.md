# Starter Site

一个可以直接部署的个人网站 / 接单展示页。

## 文件结构

- `index.html`：页面内容
- `styles.css`：页面样式
- `assets/hero.png`：首页主视觉图片
- `README-上线步骤.md`：Netlify / Vercel 上线说明

## 本地预览

直接用浏览器打开 `index.html` 即可。

## 部署

这个项目是纯静态网站，不需要构建命令。

Netlify 或 Vercel 设置：

- Build command：留空
- Publish directory：留空或填写 `.`
